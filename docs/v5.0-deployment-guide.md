# YoYu v5.0 — Phase 4 部署准备清单

> 日期：2026-06-20 · 负责人：Linus · 状态：待老板执行

## 一、GitHub Rename：fishgrow → yoyu

### 当前状态
- 仓库：https://github.com/hnlisf/fishgrow
- main 分支 HEAD：936f352（Phase 1 修复后）
- feat/phase2-rebrand：8ea2e23（Phase 2，5 commits ahead）
- fix/phase2-tank-unique-constraint：5a894c2（Phase 2 DB 约束修复）
- feat/v5-swim-feed-weather：e55bd78（Phase 3，3 commits）

### 重命名步骤（严格顺序，不可逆）

| 步骤 | 操作 | 执行人 | 预计耗时 |
|------|------|--------|----------|
| 1 | 备份所有 webhook / 集成配置（Vercel / Railway / Clerk / 监控） | 老板 | 5 min |
| 2 | 在测试 repo 预演一遍（可选但推荐） | Linus | 10 min |
| 3 | GitHub Settings → Rename repository：`hnlisf/fishgrow` → `hnlisf/yoyu` | **老板手动** | 1 min |
| 4 | 等待 GitHub 完成（自动，< 1 分钟） | - | 1 min |
| 5 | 验证 redirect：`https://github.com/hnlisf/fishgrow` → `https://github.com/hnlisf/yoyu` | 老板 | 1 min |
| 6 | 更新所有 webhook（指向新 URL） | 老板 | 5 min |

### 需要更新的第三方集成

| 平台 | 当前 | 切换后 | 负责人 |
|------|------|--------|--------|
| GitHub Repo | `hnlisf/fishgrow` | `hnlisf/yoyu` | 老板 |
| Vercel 项目 | `fishgrow` | `yoyu`（新建） | 老板 |
| Railway 项目 | `fishgrow` | `yoyu`（新建） | 老板 |
| GitHub Webhooks | `...repos/hnlisf/fishgrow/hooks` | 改为 `...repos/hnlisf/yoyu/hooks` | 老板 |
| 飞书文档链接 | `github.com/hnlisf/fishgrow` | 改为 `github.com/hnlisf/yoyu` | Linus |

### 风险缓解

| 风险 | 缓解 |
|------|------|
| 执行时间窗口 | 选用户最少时段 |
| 不可逆 | 老板手动执行，Linus 仅备份 |
| 回滚 | GitHub 不支持 rename 回滚，只能新建 fishgrow repo |
| Issue/PR 引用断裂 | `hnlisf/fishgrow#123` 会失效（当前无外部 Issue/PR） |

---

## 二、Vercel 部署切换

### 环境变量清单（需复制到新项目）

| 变量 | 说明 |
|------|------|
| `NEXT_PUBLIC_API_URL` | 后端 API 地址（Railway URL） |
| `NEXT_PUBLIC_APP_NAME` | `YoYu` |
| 其他 Next.js 环境变量 | 按需 |

### 步骤
1. Vercel Dashboard → New Project → Import `hnlisf/yoyu`
2. Framework：Next.js（自动检测）
3. Root Directory：`frontend`
4. 复制以上环境变量
5. 触发首次部署
6. 配置 custom domain（如有）

---

## 三、Railway 部署切换

### 环境变量清单

| 变量 | 说明 |
|------|------|
| `DATABASE_URL` | Postgres 连接字符串（生产） |
| `JWT_SECRET` | 如果有 auth |
| 其他后端 env | 按需 |

### 步骤
1. Railway Dashboard → New Service → GitHub Repo `hnlisf/yoyu`
2. Root Directory：`backend`
3. 复制环境变量
4. 运行 `npx prisma migrate deploy`
5. 验证 `/api` health check

---

## 四、v5.0 合并策略

```bash
git checkout main
git merge feat/phase2-rebrand      # Phase 2：品牌改名 + 首页 + 三端 + gap
git merge fix/phase2-tank-unique-constraint  # Phase 2：DB unique 约束
git merge feat/v5-swim-feed-weather  # Phase 3：游动 + 喂食 + 天气 + 偏好

# 打 tag
git tag v5.0.0
git push origin main
git push origin v5.0.0
```

### 合并前检查清单
- [ ] 前端 `next build` 0 error
- [ ] 后端 `nest build` 0 error
- [ ] 前端 `tsc --noEmit` 0 error（可选）
- [ ] Prisma migration 已同步
- [ ] GitHub rename 已完成
- [ ] Vercel/Railway 已切换
- [ ] 老板确认可以合并
